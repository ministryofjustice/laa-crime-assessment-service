apiVersion: v1
kind: Service
metadata:
  name: {{ include "crime-assessment-service.fullname" . }}
  labels:
    {{- include "crime-assessment-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.actuator.port }}
      targetPort: actuator
      protocol: TCP
      name: actuator
  selector:
    {{- include "crime-assessment-service.selectorLabels" . | nindent 4 }}
