apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "crime-assessment-service.fullname" . }}-test-connection"
  labels:
    {{- include "crime-assessment-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "crime-assessment-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
